orm:
  singular: Server
  plural: Servers
type: object
properties:
  name:
    type: string
  description:
    type: string
  type:
    type: string
    enum: [ phycisal, virtual, cluster ]
  hosts:
    type: array
    items:
      $ref: deployment_target.schema.yaml
