orm:
  singular: Server
  plural: Servers
type: object
properties:
  name:
    type: string
  description:
    type: string
  type:
    type: string
    enum: [ phycisal, virtual, cluster ]
  serves:
    type: array
    items:
      $ref: deployment_target.schema.yaml
